let println = @builtin[println].

let factorial = lambda (x) if   x == 0
						   then 1
						   else x * this(x - 1).

println(factorial(7)). % should be 4040

[-
  multiline comment
-]

let Object = @struct[a, b, c].
let o = Object(1, 2, 3).
println(o'b).

println("string literal!")
