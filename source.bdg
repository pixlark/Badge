@import[prelude].
@import[math].

func deepFunction(x, y) {
    on flag println("Flag!").
    x + (2 * y)
}.

println(deepFunction(2, 3) $ flag: 15,
                             other: deepFunction(1, 2)).
