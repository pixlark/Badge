@export[print, println, factorial].

let print = @builtin[print].
let println = @builtin[println].
let factorial = lambda (x) if x == 0
                           then 1
                           else x * this(x - 1).
