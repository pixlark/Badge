(@) source

let println = @builtin[println].

let GCD = lambda (a, b)
	if a == b
	then a
	elif a > b
	then GCD(a - b, b)
	else GCD(a, b - a).

println(GCD(1814274, 259896)).

(@) expected(0, stdout)
4998
